# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is a fresh scaffold with no source code yet. It currently contains only `README.md`, `LICENSE`, and a `.gitignore`. Per the README, its purpose is:

> Keeping up with the current understanding and practices of AI and application security.

There is no build system, package manifest, lint config, or test suite in place — do not assume any particular language or framework. The `.gitignore` is a generic Python template, but no Python project has been initialized (no `pyproject.toml`, `requirements.txt`, etc.), so treat that as a placeholder rather than a signal of the intended stack.

When adding the first real code to this repo, confirm the intended language/framework and tooling with the user, then update this file with the actual commands (build, lint, test) and architecture notes.
